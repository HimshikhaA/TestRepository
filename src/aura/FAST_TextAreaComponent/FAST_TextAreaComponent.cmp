<!--
 - Created by cedrickithima on 30/10/2017.
 -->

<aura:component description="FAST_TextAreaComponent">

    <!-- Attributes -->
    <aura:attribute name="inputLabel" type="String" default=""/>
    <aura:attribute name="inputName" type="String" default="text"/>
    <aura:attribute name="inputValue" type="String" default=""/>
    <aura:attribute name="initialValue" type="String" default=""/>
    <aura:attribute name="customLabelID" type="String" default=""/>
    <aura:attribute name="inputMaxLength" type="Integer" default="3000"/>
    <aura:attribute name="placeholder" type="String" default=""/>
    <aura:attribute name="inputDisabled" type="Boolean" default="false"/>
    <aura:attribute name="inputRequired" type="Boolean" default="false"/>

    <!-- Events -->
    <aura:registerEvent name="fastInputEvent" type="c:FAST_InputEvent"/>
    <aura:handler name="init" value="{!this}" action="{!c.doInit}"/>
    <aura:handler name="change" value="{!v.inputValue}" action="{!c.handleInputEvent}" />

    <lightning:layout multipleRows="true">
        <lightning:layoutItem padding="around-small" size="12" >
            <div id="{!v.inputName}">
                <lightning:textarea name="{!v.inputName}" value="{!v.inputValue}"
                                     label="{!v.inputLabel}" maxlength="{!v.inputMaxLength}"  placeholder="{!v.placeholder}" disabled="{!v.inputDisabled}" required="{!v.inputRequired}"/>

            </div>
        </lightning:layoutItem>

    </lightning:layout>

</aura:component>